import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Settings',
  description:
    'Tune your typrlab practice — content mode, key-introduction order, target speed, themes, typing font, and text size.',
  alternates: { canonical: '/settings' },
};

export default function SettingsPage() {
  return null;
}
