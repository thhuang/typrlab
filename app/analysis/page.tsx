import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Analysis',
  description:
    'Your typing progress dashboard — speed and accuracy over time, a per-key speed heatmap, your slowest keys and transitions, and a practice calendar.',
  alternates: { canonical: '/analysis' },
};

export default function AnalysisPage() {
  return null;
}
