// Route shells. The persistent layout renders the app (AppClient), which derives
// the active view from the pathname — so these pages just register the routes.
export default function PracticePage() {
  return null;
}
